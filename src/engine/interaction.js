import{state}from"./state.js";import{BRICK_TYPES,COLORS}from"./constants.js";import{world,currentBuildTarget}from"./scene.js";import{createBrick}from"./bricks.js";import{addBrickToChunk,removeBrick,bricksGlobal}from"./chunks.js";import{undoManager}from"./undo.js";function root(o){let t=o;while(t.parent&&t.parent.type!=="Scene")t=t.parent;return t}
export function performAction(){if(state.mode==="BUILD"){if(!world.ghost.visible)return false;const bt=BRICK_TYPES[state.brickTypeIdx],b=createBrick(COLORS[state.colorIdx].hex,false,bt,state.brickRot);b.position.copy(world.ghost.position);b.userData.colorIdx=state.colorIdx;b.userData.rot=state.brickRot;addBrickToChunk(world.scene,b);undoManager.recordAdd(b);return true}else{const hit=currentBuildTarget();if(!hit||hit.object.userData?.isFloor)return false;const b=root(hit.object);if(!bricksGlobal.includes(b))return false;undoManager.recordRemove(b);removeBrick(world.scene,b);return true}}
export function rotateSelection(){state.brickRot=state.brickRot?0:1}