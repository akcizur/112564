const THREE=window.THREE;import{UNIT,BRICK_H,PLATE_H,BRICK_TYPES,COLORS}from"./constants.js";import{uid}from"./uid.js";const studGeo=new THREE.CylinderGeometry(.35,.35,.15,12);
export function createBrick(color,isGhost,brickType,rotation=0){const h=brickType.type==="brick"?BRICK_H:PLATE_H,g=new THREE.Group(),mat=isGhost?new THREE.MeshBasicMaterial({color,transparent:true,opacity:.42}):new THREE.MeshStandardMaterial({color,roughness:.42,metalness:.06}),w=rotation?brickType.l:brickType.w,l=rotation?brickType.w:brickType.l,b=new THREE.Mesh(new THREE.BoxGeometry(w*UNIT,h,l*UNIT),mat);if(!isGhost){b.castShadow=true;b.receiveShadow=true;b.scale.set(.99,.99,.99)}g.add(b);const ox=(w-1)*UNIT/2,oz=(l-1)*UNIT/2;for(let x=0;x<w;x++)for(let z=0;z<l;z++){const s=new THREE.Mesh(studGeo,mat);s.position.set(-ox+x*UNIT,(h+.15)/2,-oz+z*UNIT);g.add(s)}g.userData={brickUuid:uid("brick"),typeId:brickType.id,w,l,h,colorIdx:0,rot:rotation};return g}
export function serializeBrick(b){return{uuid:b.userData.brickUuid,typeId:b.userData.typeId,colorIdx:b.userData.colorIdx??0,rot:b.userData.rot??0,pos:[b.position.x,b.position.y,b.position.z]}}
export function createBrickFromData(d){const bt=BRICK_TYPES.find(x=>x.id===d.typeId);if(!bt)return null;const c=COLORS[d.colorIdx]||COLORS[0],b=createBrick(c.hex,false,bt,d.rot||0);b.userData.brickUuid=d.uuid||uid("brick");b.userData.colorIdx=Number.isInteger(d.colorIdx)?d.colorIdx:0;b.userData.rot=d.rot||0;b.position.set(...(d.pos||[0,(bt.type==="brick"?BRICK_H:PLATE_H)/2,0]));return b}