import{MAX_UNDO}from"./constants.js";import{serializeBrick,createBrickFromData}from"./bricks.js";import{addBrickToChunk,removeBrick,getBrickById}from"./chunks.js";
export const undoManager={undoStack:[],redoStack:[],push(a){this.undoStack.push(a);if(this.undoStack.length>MAX_UNDO)this.undoStack.shift();this.redoStack.length=0},recordAdd(b){this.push({type:"ADD",brick:serializeBrick(b)})},recordRemove(b){this.push({type:"REMOVE",brick:serializeBrick(b)})},undo(scene){const a=this.undoStack.pop();if(!a)return false;if(a.type==="ADD"){const b=getBrickById(a.brick.uuid);if(b)removeBrick(scene,b)}else{const b=createBrickFromData(a.brick);if(b)addBrickToChunk(scene,b)}this.redoStack.push(a);return true},redo(scene){const a=this.redoStack.pop();if(!a)return false;if(a.type==="ADD"){const b=createBrickFromData(a.brick);if(b)addBrickToChunk(scene,b)}else{const b=getBrickById(a.brick.uuid);if(b)removeBrick(scene,b)}this.undoStack.push(a);return true},serialize(){return{undo:this.undoStack,redo:this.redoStack}},deserialize(d){this.undoStack=Array.isArray(d?.undo)?d.undo:[];this.redoStack=Array.isArray(d?.redo)?d.redo:[]},clear(){this.undoStack.length=0;this.redoStack.length=0}};